{% extends 'baseback.html.twig' %}

{% block title %}event categories{% endblock %}

{% block body %}
<div class="main-panel">
  <div class="content-wrapper">
    <div class="row">

      <div class="col-lg-6 grid-margin stretch-card">
        <div class="card">
          <div class="card-body">
            <h4 class="card-title">category</h4>
            <p class="card-description">ajouter une categorie</p>
            <div class="table-responsive">
              <table class="table table-striped">
                <thead>
                  <tr>
                    <th>Image</th> <!-- Nouvelle colonne pour l'image -->
                    <th>Name</th>
                    
                  </tr>
                </thead>
                <tbody>
                  
                  {% for c in categories %}
                      <tr>
                        <td>
                          <img src="{{  asset('uploads/' ~ c.description )  }}" class="img-fluid rounded-3" style="width: 50px; height: 50px;">
                        </td> <!-- Affichage de l'image -->
                        <td>{{ c.nom }}</td>
                        
                        <td>
                          <a href="{{ path('category_edit', {'id': c.id_categorie}) }}" class="btn btn-sm btn-outline-warning me-2"><i class="fa fa-pencil"></i></a>

                          <a href="{{ path('category_delete', {'id': c.id_categorie}) }}" class="btn btn-sm btn-outline-danger" onclick="return confirm('Are you sure you want to delete this item?');"><i class="fa fa-trash"></i></a>
                        </td>
                      </tr>
                    
                  {% endfor %}
                  
                </tbody>
              </table>
            </div>
          </div>
        </div>
      </div>

      

    </div>
  </div>
</div>
{% endblock %}
