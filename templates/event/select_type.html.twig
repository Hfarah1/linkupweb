{% extends 'base.html.twig' %}

{% block title %}Créer un événement{% endblock %}

{% block body %}
<div class="container mt-4">
    <nav aria-label="breadcrumb">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="{{ path('eventslist') }}">Événements</a></li>
            <li class="breadcrumb-item active">Choisir le type d'événement</li>
        </ol>
    </nav>

    <div class="text-center mb-5">
        <h1 class="display-4">Créer un nouvel événement</h1>
        <p class="lead text-muted">Pour la catégorie : {{ categorie.nom }}</p>
    </div>
    
    <div class="row justify-content-center g-4">
        <div class="col-md-5">
            <div class="card h-100 shadow-sm hover-card">
                <div class="card-body text-center p-5">
                    <i class="fas fa-video fa-4x text-primary mb-3"></i>
                    <h3 class="card-title">Événement Virtuel</h3>
                    <p class="card-text">Créez une session en ligne avec vos participants via notre plateforme de visioconférence intégrée.</p>
                    <a href="{{ path('event_new', {'id_categorie': id_categorie, 'type': 'virtual'}) }}" class="btn btn-primary btn-lg mt-3">
                        <i class="fas fa-plus-circle"></i> Créer un événement virtuel
                    </a>
                </div>
            </div>
        </div>
        
        <div class="col-md-5">
            <div class="card h-100 shadow-sm hover-card">
                <div class="card-body text-center p-5">
                    <i class="fas fa-map-marker-alt fa-4x text-success mb-3"></i>
                    <h3 class="card-title">Événement Présentiel</h3>
                    <p class="card-text">Organisez une rencontre physique avec vos participants dans un lieu défini.</p>
                    <a href="{{ path('event_new', {'id_categorie': id_categorie, 'type': 'physical'}) }}" class="btn btn-success btn-lg mt-3">
                        <i class="fas fa-plus-circle"></i> Créer un événement présentiel
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
.hover-card {
    transition: transform 0.2s ease-in-out, box-shadow 0.2s ease-in-out;
}
.hover-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 .5rem 1rem rgba(0,0,0,.15)!important;
}
</style>
{% endblock %} 