{% extends 'baseback.html.twig' %}

{% block title %}Edit Product{% endblock %}

{% block body %}
  <style>
        body {
            color: #566787;
            background: #f5f5f5;
            font-family: 'Varela Round', sans-serif;
            font-size: 13px;
        }
        .wrapper {
            width: 500px;
            margin: 0 auto;
            padding-top: 20px;
        }
        .btn-custom {
            background: #299be4;
            color: #ffffff;
        }
        .btn-custom:hover {
            background: #1c8cd7;
            color: #ffffff;
        }
    </style>

 {{ include('event/form.html.twig', {'button_label': 'Update'}) }}
{% endblock %}
